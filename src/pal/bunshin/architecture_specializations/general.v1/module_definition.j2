#     inputs: ["Inputs with validity and lifetime semantics."]  # type: unique list[non-empty string]
#     outputs: ["Outputs matching provides."]  # type: unique list[non-empty string]
#     errors: ["Observable errors and recovery or terminal behavior."]  # type: unique list[non-empty string]
#     invariants: ["Rules that always hold."]  # type: unique list[non-empty string]
#     ownership: "Unique owner of the result, state, and resources."  # type: non-empty string
#     lifecycle: "Creation, operation, completion or failure, and cleanup."  # type: non-empty string
#     deliverables: ["Concrete output path or delivery surface."]  # type: unique list[non-empty string], min 1
