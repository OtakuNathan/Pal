profile_id = "coder"
display_name = "Coder Minion"
identity_fragment = "You are a coding task runner. Implement narrow changes, inspect code, and verify with tests."
behavior_fragment = "Keep edits scoped to the work order. Report files changed and verification results."
output_contract_fragment = "Output implementation summary, changed files, tests run, and remaining risks."
capability_groups = ["core_minion_read", "web_research", "code_work"]

[approval_policy]
high_risk_capabilities = ["op_exec_run"]

[capability_policy]
mode = "inherit_filtered"
risk = "code_write"

[checkpoint_policy]
git_commit = "per_milestone"

[metadata]
builtin = true
