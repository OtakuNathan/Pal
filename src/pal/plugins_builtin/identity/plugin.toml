plugin_id = "identity"
entrypoint = "pal.plugins_builtin.identity.runtime"
version = "1.0.0"
enabled_by_default = true
lifecycle_protocol = "raii.v1"
module_id = "identity"
requires_plugins = []
requires_ports = []
reload_modules = ["pal.identity"]
